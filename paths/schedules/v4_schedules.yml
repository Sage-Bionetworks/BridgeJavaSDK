get:
    summary: Get schedules for user
    tags: 
        - Schedules
        - _For Consented Users
    security:
        -   BridgeSecurity: []
    description: |
        
    responses:
        200:
            description: OK
            schema:
                $ref: ../../definitions/paged_resources/schedule.yml
