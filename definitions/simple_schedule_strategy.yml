title: SimpleScheduleStrategy
allOf:
    - $ref: ./schedule_strategy.yml
    - type: object
    - required:
        - schedule
        - type
    - properties:
        schedule:
            $ref: ./schedule.yml
