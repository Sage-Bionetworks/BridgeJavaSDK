type: object
required:
    - recordIds
    - synapseExporterStatus
properties:
    recordIds:
        type: array
        items:
            type: string
    synapseExporterStatus:
        type: string
        enum:
            - not_exported
            - succeeded
    type:
        type: string
        readOnly: true
        enum: [RecordExportStatusRequest]
