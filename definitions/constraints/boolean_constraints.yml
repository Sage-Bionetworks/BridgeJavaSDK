description: |
    **UI hints:** checkbox, toggle.
allOf:
    - $ref: ./constraints.yml
    - properties:
        dataType:
            type: string
            enum: [boolean]
        type:
            type: string
            enum: [BooleanConstraints]
