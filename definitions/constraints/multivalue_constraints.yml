allOf:
    - $ref: ./constraints.yml
    - properties:
        allowOther:
            type: boolean
        allowMultiple:
            type: boolean
        enumeration:
            type: array
            items:
                $ref: ../survey_question_option.yml
        dataType:
            type: string
            enum: [multivalue]
        type:
            type: string
            enum: [MultiValueConstraints]
