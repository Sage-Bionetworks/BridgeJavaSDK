allOf:
    - $ref: ./constraints.yml
    - properties:
        minLength:
            type: integer
        maxLength:
            type: integer
        pattern:
            type: string
        dataType:
            type: string
            enum: [string]
        type:
            type: string
            enum: [StringConstraints]
