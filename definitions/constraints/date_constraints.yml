title: DateConstraints
allOf:
    - $ref: ./constraints.yml
    - properties:
        earliestValue:
            type: string
            format: date
        latestValue:
            type: string
            format: date
