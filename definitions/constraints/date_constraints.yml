allOf:
    - $ref: ./constraints.yml
    - properties:
        earliestValue:
            type: string
            format: date
        latestValue:
            type: string
            format: date
        dataType:
            type: string
            enum: [date]
        type:
            type: string
            enum: [DateConstraints]
