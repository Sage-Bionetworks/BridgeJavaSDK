allOf:
    - $ref: ./constraints.yml
    - properties:
        earliestValue:
            type: string
            format: date-time
        latestValue:
            type: string
            format: date-time
        dataType:
            type: string
            enum: [datetime]
        type:
            type: string
            enum: [DateTimeConstraints]
