description: |
    **UI hints:** datetimepicker.
allOf:
    - $ref: ./constraints.yml
    - properties:
        allowFuture:
            type: boolean
            default: false
            description: Should this question allow answers in the future?
        earliestValue:
            type: string
            format: date-time
        latestValue:
            type: string
            format: date-time
        dataType:
            type: string
            enum: [datetime]
        type:
            type: string
            enum: [DateTimeConstraints]
