type: object
required:
    - identifier
properties:
    identifier:
        type: string
        description: The report identifier.
    type:
        type: string
        readOnly: true
        enum: [ReportIndex]