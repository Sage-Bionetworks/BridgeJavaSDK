allOf:
    - $ref: './abstract_study_participant.yml'
    - type: object
    - required:
        - study
        - email
        - password
    - properties:
        study:
            type: string
        password:
            type: string
        type:
            type: string
            enum: [SignUp]
