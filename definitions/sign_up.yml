allOf:
    - $ref: './abstract_study_participant.yml'
    - type: object
    - required:
        - study
        - email
        - password
    - properties:
        study:
            type: string
        password:
            type: string
            format: password
        type:
            type: string
            readOnly: true
            enum: [SignUp]
