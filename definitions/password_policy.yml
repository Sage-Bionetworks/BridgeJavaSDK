type: object
properties:
    minLength:
        type: integer
        minimum: 1
        default: 8
    numericRequired:
        type: boolean
    symbolRequired:
        type: boolean
    lowerCaseRequired:
        type: boolean
    upperCaseRequired:
        type: boolean
    