type: object
properties:
    items:
        type: array
        readOnly: true
        items:
            $ref: ../study.yml
    total:
        type: integer
        readOnly: true
        description: The total number of records in the items list
    type:
        type: string
        readOnly: true
        enum: [ResourceList]
