type: object
required:
    - items
    - total
    - type
properties:
    items:
        type: array
        items:
            $ref: ../study.yml
    total:
        type: integer
        description: The total number of records in the items list
    type:
        type: string
        enum: [ResourceList]
