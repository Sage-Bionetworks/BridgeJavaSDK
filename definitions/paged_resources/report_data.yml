type: object
required:
    - items
    - startDate
    - endDate
    - total
    - type
properties:
    items:
        type: array
        items:
            $ref: ../report_data.yml
    startDate:
        type: string
        format: date
    endDate:
        type: string
        format: date
    total:
        type: integer
    type:
        type: string
        enum: [DateRangeResourceList]