type: object
readOnly: true
properties:
    items:
        type: array
        items:
            $ref: ../study_consent.yml
    total:
        type: integer
        description: The total number of records in the items list
    type:
        type: string
        readOnly: true
        enum: [ResourceList]
