title: ResourceList<Survey>
type: object
required:
    - items
    - total
    - type
properties:
    items:
        type: array
        items:
            $ref: ../survey.yml
    total:
        type: integer
        description: The total number of records that match the criteria (may exceed page size)
    type:
        type: string
        enum: [ResourceList]
