type: object
required:
    - items
    - total
    - type
properties:
    items:
        type: array
        items:
            $ref: ../upload_schema.yml
    total:
        type: integer
        description: The total number of records in the list
    type:
        type: string
        enum: [ResourceList]
        
