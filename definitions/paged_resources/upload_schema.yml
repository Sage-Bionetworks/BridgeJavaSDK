type: object
properties:
    items:
        type: array
        readOnly: true
        items:
            $ref: ../upload_schema.yml
    total:
        type: integer
        readOnly: true
        description: The total number of records in the list
    type:
        type: string
        readOnly: true
        enum: [ResourceList]
