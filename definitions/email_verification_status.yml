type: object
required:
    - status
properties:
    status:
        type: string
        description: Status of verifying support email address. 
        enum:
            - unverified
            - pending
            - verified
    type:
        type: string
        readOnly: true
        enum: [EmailVerificationStatus]
