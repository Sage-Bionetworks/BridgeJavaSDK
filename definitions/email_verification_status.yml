type: object
required:
    - status
    - type
properties:
    status:
        type: string
        description: Status of verifying support email address. 
        enum:
            - unverified
            - pending
            - verified
    type:
        type: string
        enum: [EmailVerificationStatus]
