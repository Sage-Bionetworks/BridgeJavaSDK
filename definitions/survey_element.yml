type: object
description: A survey element (info screen or question)
discriminator: type 
required:
    - guid
    - identifier
    - type
properties:
    guid:
        type: string
    identifier:
        type: string
    # type:
    #     type: string
    #     enum:
    #         - SurveyQuestion
    #         - SurveyInfoScreen
