description: |
    A response to submission of a JSON payload that is not valid. The errors 
type: object
required:
    - statusCode
    - errors
    - entityClass
    - entity
properties:
    statusCode:
        type: integer
    errors:
        type: array
        items:
            type: object
            additionalProperties:
                type: array
                items:
                    type: string
    entityClass:
        type: string
    entity:
        type: object