title: Email
type: object
required:
    - name  
    - studyIdentifier
properties:
    email:
        type: string
    studyIdentifier:
        type: string  
    type:
        type: string
        enum: [Email]