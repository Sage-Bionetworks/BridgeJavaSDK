type: object
required:
    - guid
    - label
    - strategy
properties:
    guid:
        type: string
    label:
        type: string
    modifiedOn:
        type: string
        format: date-time
    strategy:
        $ref: ./schedule_strategy.yml
    version:
        type: integer
    type:
        type: string
        readOnly: true
        enum: [SchedulePlan]
    