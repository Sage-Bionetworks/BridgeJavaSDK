type: object
required:
    - label
    - type
properties:
    label:
        type: string
    detail:
        type: string
    value:
        type: string
    image:
        $ref: ./image.yml
    type:
        type: string
        enum: [SurveyQuestionOption]