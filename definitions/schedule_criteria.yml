type: object
required:
    - criteria
    - schedule
properties:
    criteria:
        $ref: ./criteria.yml
    schedule:
        $ref: ./schedule.yml
    type:
        type: string
        readOnly: true
        enum: [ScheduleCriteria]