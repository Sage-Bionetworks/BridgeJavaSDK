type: object
required:
    - criteria
    - schedule
    - type
properties:
    criteria:
        $ref: ./criteria.yml
    schedule:
        $ref: ./schedule.yml
    type:
        type: string
        enum: [ScheduleCriteria]