get:
    operationId: getUsersStudyParticipantWeeklyAdherenceReport
    summary: Get a detailed report on the caller’s current week of scheduled activities.
    tags:
        - Study Participants
        - Adherence Records
        - _For Consented Users
    parameters:
        - $ref: ../../index.yml#/parameters/studyId
    responses:
        200:
            description: OK
            schema:
                $ref: ../../definitions/weekly_adherence_report.yml
        401:
            $ref: ../../responses/401.yml
        412:
            $ref: ../../responses/412.yml