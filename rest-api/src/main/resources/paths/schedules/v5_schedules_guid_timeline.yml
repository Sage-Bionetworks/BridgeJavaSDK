get:
    operationId: getTimelineForSchedule
    summary: Get the timeline generated by a schedule
    tags: 
        - Schedules (v2)
        - _For Developers
        - _For Study Designers
    security:
        - BridgeSecurity: []
    parameters:
        - $ref: ../../index.yml#/parameters/guid
    description: Get a timeline associated with a schedule. For this call, the schedule does not need to be published.
    responses:
        200:
            description: OK
            schema:
                $ref: ../../definitions/timeline.yml
        401:
            $ref: ../../responses/401.yml
        403:
            $ref: ../../responses/403_not_study_designer_developer.yml