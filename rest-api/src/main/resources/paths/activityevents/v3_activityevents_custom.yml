post:
    operationId: createCustomActivityEvent
    summary: Create a study-specific ActivityEvent
    description: Create a custom ActivityEvent record. The eventKey must be whitelisted on the Study.
    tags: 
        - ActivityEvents
        - _For Consented Users
    parameters:
        - name: body
          required: true
          in: body
          schema:
            $ref: ../../definitions/custom_activity_event_request.yml
    responses:
        200:
            $ref: ../../responses/200_message.yml