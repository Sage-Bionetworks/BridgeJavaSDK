type: string
description: |
    |Enum|Description|
    |---|---|
    |asc| Sort earliest startedOn time to most recent startedOn timestamp. |
    |desc| Sort most recent startedOn time to earliest startedOn timestamp. |
enum:
    - asc
    - desc
