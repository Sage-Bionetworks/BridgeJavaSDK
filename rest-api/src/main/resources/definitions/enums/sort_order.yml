type: string
description: |
    |Enum|Description|
    |---|---|
    |asc| Sort so that the earliest startedOn time is the first record in the returned list. |
    |desc| Sort so that the most recent startedOn time is the first record in the returned list. |
enum:
    - asc
    - desc
