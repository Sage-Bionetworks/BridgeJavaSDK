type: string
description: |
    They type of notification to provide relative to a session’s time window. This enum specifies when the notification should be shown to the participant.

    |Status|Description|
    |---|---|
    |after_window_start|Issue the notification the `offset` after the window starts|
    |before_window_end|Issue the notification the `offset` before the window ends|
enum:
    - after_window_start
    - before_window_end