description: |
    The status of the email address that will be used to send consents to users. The consents are sent on behalf of the app using the app's support email address, and our email provider (Amazon's SES) requires that the address holder confirm this is an allowed use of the address. 
type: object
properties:
    status:
        $ref: ./enums/account_status.yml
        readOnly: true
        x-nullable:  false
    type:
        type: string
        readOnly: true
        description: "EmailVerificationStatus"
        x-nullable:  false
