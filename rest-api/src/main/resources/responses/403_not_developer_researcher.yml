description: |
    User cannot access this service because they are not a developer or researcher (no developer or researcher role on account, or a developer is attempting an operation on a non-test account that is not the developer account itself).
schema:
    $ref: ../definitions/message.yml
