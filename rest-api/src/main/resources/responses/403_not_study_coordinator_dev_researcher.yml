description: |
    User cannot access this service because they do not have an administrative role (no study coordinator, developer, or researcher role on account).
schema:
    $ref: ../definitions/message.yml
