description: |
    User cannot access this service because they do not have an administrative role (no study coordinator, org administrator, or admin role on account).
schema:
    $ref: ../definitions/message.yml
