description: |
    User cannot access this service because they do not have an administrative role (no developer, researcher, or org admin role on account).
schema:
    $ref: ../definitions/message.yml
