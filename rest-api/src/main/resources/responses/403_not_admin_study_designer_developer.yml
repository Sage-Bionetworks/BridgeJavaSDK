description: |
    User cannot access this service because they do not have an administrative role (no admin, study designer who is a member of the organization that owns the object, or developer role on account).
schema:
    $ref: ../definitions/message.yml
