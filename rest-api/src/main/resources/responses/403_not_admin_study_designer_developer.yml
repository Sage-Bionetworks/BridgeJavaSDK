description: |
    User cannot access this service because they do not have an administrative role (no admin, study designer, or developer role on account).
schema:
    $ref: ../definitions/message.yml
