description: |
    This endpoint has not been implemented for this app. It has been disabled, or not completely configured. 
    The caller cannot fix the call to succeed, but it is not an unexpected server error, it is intentional.
schema:
    $ref: ../definitions/message.yml
